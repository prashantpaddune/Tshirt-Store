import React from "react";
import Base from "../core/Base";

const Profile = () => {
    return(
        <Base title="Profile" description="My profile !">
            <h1>Profile page !</h1>
        </Base>
    )
}

export default Profile;